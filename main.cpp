#include <iostream>
#include "Concurrent_Control_Component/Semaphore.h"









int main() {

    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    std::cout.tie(0);
    Semaphore_Test::test();

    return 0;
}